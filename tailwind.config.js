/** @type {import('tailwindcss').Config} */
export default {
  darkMode: 'class',  // Use 'class' so dark mode is activated with the 'dark' class
  content: ["./src/**/*.{html,js,svelte,ts}"],
  theme: {
    extend: {},
  },
  plugins: [],
};
